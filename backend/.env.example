# ==============================================
# ANITS Student Late Tracking System
# Environment Configuration
# ==============================================

# ==================
# SERVER CONFIGURATION
# ==================
PORT=5000
NODE_ENV=development

# ==================
# DATABASE CONFIGURATION
# ==================
# For local development with MongoDB running locally:
MONGODB_URI=mongodb://localhost:27017/studentLateTracking

# For college server (replace with actual college MongoDB connection):
# MONGODB_URI=mongodb://college-server-ip:27017/studentLateTracking

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/studentLateTracking?retryWrites=true&w=majority

# Legacy support (MONGO_URI will be used as fallback):
MONGO_URI=mongodb://localhost:27017/studentLateTracking

# ==================
# JWT AUTHENTICATION
# ==================
# IMPORTANT: Change this to a strong, unique secret in production!
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-crypto-random-bytes

# ==================
# EMAIL SERVICE CONFIGURATION (for OTP)
# ==================
# Email service provider (Gmail, Outlook, or custom SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# Faculty email credentials for sending OTPs
# For Gmail: Use App Password (not regular password)
# Generate App Password: Google Account → Security → 2-Step Verification → App passwords
EMAIL_USER=your-email@anits.edu.in
EMAIL_PASSWORD=your-app-password-here

# ==================
# OPTIONAL: FRONTEND URL
# ==================
# Used for CORS and email links
FRONTEND_URL=http://localhost:3000

# ==================
# DEPLOYMENT NOTES
# ==================
# 1. For college server deployment:
#    - Update MONGODB_URI with college server details
#    - Ensure MongoDB is accessible from the application server
#    - Set strong JWT_SECRET using crypto.randomBytes(64).toString('hex')
#    - Configure email settings with college SMTP server
#
# 2. Security recommendations:
#    - Never commit .env file to version control
#    - Use strong passwords and secrets
#    - Restrict database access to application server IP only
#    - Enable MongoDB authentication
#    - Use HTTPS in production
#
# 3. Email service setup:
#    - Option 1: Use Gmail with App Password (for development/testing)
#    - Option 2: Use college SMTP server (recommended for production)
#    - Option 3: Use email service provider (SendGrid, AWS SES, etc.)
#
# 4. Database migration from existing system:
#    - Export data from old system
#    - Import to new MongoDB following new schema
#    - Or configure connection to existing college database
